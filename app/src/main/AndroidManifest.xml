<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ua.gov.mva.vfaces">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <supports-screens
        android:largeScreens="false"
        android:largestWidthLimitDp="720"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true"/>

    <application
        android:name=".VFacesApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <!-- Launcher Activity -->
        <activity
            android:name=".presentation.ui.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Other Activities -->
        <activity
            android:name=".presentation.ui.auth.AuthActivity"
            android:theme="@style/AppThemeNoActionBar"
            android:screenOrientation="portrait" />
        <activity
            android:name=".presentation.ui.questionnaire.create.CreateQuestionnaireActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".presentation.ui.questionnaire.list.QuestionnaireListActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>